# /\ogos — Мінімалістичний блог-движок


## 🎯 Особливості

- **Абсолютний мінімалізм** — жодних фреймворків, чистий PHP
- **Фокус на типографіці** — Georgia для лого, system fonts для контенту
- **Процедурний код** — без класів і OOP, максимальна простота
- **720px ширина** — ідеальна читабельність
- **Темна тема** — автоматична підтримка `prefers-color-scheme`
- **Швидкість** — мінімальні запити до БД, без JS для контенту

## 📦 Встановлення

### 1. Клонування
```bash
git clone <your-repo> cms4blog
cd cms4blog
```

### 2. База даних
Імпортуйте схему (все в одному файлі):
```bash
mysql -u root -p < database.sql
```

Або через phpMyAdmin:
- Імпорт → `database.sql` (включає всі таблиці + Rose Search)

4. Створіть таблиці для пошукового індексу Rose:
```bash
php init_search_tables.php
```
Або запустіть переіндексацію, яка автоматично створить таблиці:
```bash
php reindex.php
```

### 3. Налаштування
Відредагуйте `config/db.php`:
```php
$host = 'localhost';
$db   = 'logos_db';
$user = 'root';
$pass = 'ваш_пароль';
```

### 4. Apache
Переконайтеся, що `mod_rewrite` увімкнено:
```bash
a2enmod rewrite
service apache2 restart
```

### 5. Права доступу
```bash
chmod -R 755 uploads/
chown -R www-data:www-data uploads/
```

### 6. Індексація для пошуку
Після створення постів, проіндексуйте їх:
```bash
php reindex.php
```

Або у браузері:
```
http://localhost/cms4blog/reindex.php
```

## 🚀 Використання

### Головна сторінка
```
http://localhost/cms4blog/
```

### Адмін-панель
```
http://localhost/cms4blog/admin/
```

- **Список постів**: `/admin/`
- **Новий пост**: `/admin/editor.php`
- **Редагування**: `/admin/editor.php?id=1`

### Одиночний пост
```
http://localhost/cms4blog/welcome-to-logos
```

### Пошук
```
http://localhost/cms4blog/search.php?q=мінімалізм
```

Пошук підтримує:
- Fusearch.php             # Сторінка пошуку
├── reindex.php            # Скрипт індексації для пошуку
├── database.sql           # Схема БД + тестові дані
├── database_search.sql    # Таблиці для Rose search
- Морфологічний пошук (стемінг для російської/української)
- Виділення знайдених слів у сніпетах
- Ранжування результатів за релевантністю

## 📁 Структура проекту

```
/\ogos/
├── .htaccess              # URL rewriting (based on Aegea)
├── index.php              # Front controller (роутер)
├── database.sql           # Повна схема БД (core + Rose Search)
├── reindex.php            # Скрипт індексації пошуку
├── search.php             # Сторінка пошуку
├── ├── db.php             # PDO з'єднання
│   └── autoload.php       # PSR-4 autoloader для Rose
│
├── includes/
│   ├── SearchService.php  # Wrapper для Rose search
│   └── ContentParser.php  # Wrapper для Neasden парсера
│
├── config/
│   ├── db.php             # PDO з'єднання
│   └── autoload.php       # PSR-4 autoloader для Rose + Neasden
│
├── views/
│   ├── layout.php         # Головний шаблон
│   ├── timeline.php       # Список постів
│   └── post.php           # Одиночний пост + коментарі
│
├── admin/
│   ├── index.php          # Список всіх постів
│   └── editor.php         # Форма створення/редагування
│
├── assets/
│   ├── css/
│   │   └── style.css      # Мінімалістичні стилі (Aegea-inspired)
│   ├── fonts/
│   │   └── tildasans.css  # Шрифтова родина Tilda Sans
│   └── libs/
│       ├── fotorama/      # Галерея зображень
│       ├── fontawesome/   # Іконки
│       ├── rose/          # Rose Search Engine
│       ├── neasden/       # Neasden Markup Parser
│       ├── highlight/     # Highlight.js для коду
│       └── momentjs/      # Moment.js для дат
│
└── uploads/               # Завантаження (поки не використовується)
```

## 🗄️ База даних

### Таблиці

1. **settings** — key-value налаштування (назва блогу, опис)
2. **posts** — пости з полями:
   - `id`, `slug`, `title`, `content`
   - `type` (text/image/link/quote)
   - `created_at`, `is_published`

**Rose Search Tables:**
6. **rose_toc** — Table of Contents (індексовані документи)
7. **rose_fulltext_index** — Fulltext індекс (слова + позиції)
8. **rose_keyword_index** — Keyword індекс (ключові слова + типи)
9. **rose_content** — Зберігання контенту для snippet generation
3. **comments** — коментарі з підтримкою вкладених відповідей
   - `parent_id` для replies
   - `userpic` для аватарок
4. **tags** — теги для постів
5. **post_tags** — зв'язок багато-до-багатьох

## 🎨 Дизайн

### CSS Variables
```css
--backgroundColor: #fff
--foregroundColor: #000
--hoverColor: #eb5757
--maxWidth: 720px
--noteTextFontSize: 20px
--noteTitleFontSize: 36px
```

### Шрифти
- **Лого**: Georgia (serif)
- **Контент**: Tilda Sans → system-ui, -apple-system, Roboto
- **Код**: Consolas, Courier New (monospace)

**Tilda Sans вага:**
- 300 (Light)
- 400 (Regular) — основний
- 500 (Medium)
- 600 (Semibold)
- 700 (Bold)
- 800 (ExtraBold)
- 900 (Black)

### Кольори
- **Текст**: `#000` (світла) / `#c0c0c0` (темна)
- **Акцент**: `#eb5757` (червоний)
- **Лінки**: `#0060a0` (синій)
- **Ховер**: `#eb5757`

## 🧩 Розширення

### Додати новий тип поста

**1. Оновіть enum у `database.sql`:**
```sql
ALTER TABLE `posts` MODIFY `type` ENUM('text','image','link','quote','video') DEFAULT 'text';
```

**2. Додайте опцію в `admin/editor.php`:**
```html
<option value="video">Video</option>
```

### Додати поле автора

**1. Додайте колонку:**
```sql
ALTER TABLE `posts` ADD `author_name` VARCHAR(100) AFTER `content`;
```

**2. Оновіть форму в `admin/editor.php`**

**3. Виведіть у `views/post.php`:**
```php
<?= htmlspecialchars($post['author_name']) ?>
```

## 🔧 Технології

- **Backend**: PHP 8.x (процедурний стиль)
- **База даних**: MariaDB/MySQL з PDO
- **Frontend**: Vanilla JS + jQuery 3.6.0
- **Шрифти**: Tilda Sans (woff2)
- **CSS**: Normalize.css + кастомні стилі
- **Server**: Apache з mod_rewrite
- **Галереї**: Fotorama (як в Aegea)
- **Іконки**: FontAwesome 6 Free
- **Пошук**: Rose Search Engine (by Roman Parpalak)
- **Розмітка**: Neasden (Markdown-like парсер від Ілії Бірмана)
- **Підсвічування коду**: highlight.js
- **Дати**: Moment.js з українською локаллю

## ✍️ Синтаксис Neasden

**Neasden** — власний Markdown-подібний парсер від Ілії Бірмана (автор Aegea).

### Базовий синтаксис:

```
# Заголовок першого рівня
## Заголовок другого рівня

Простий параграф тексту.

**Жирний текст** або //курсив//

- Пункт списку 1
- Пункт списку 2

> Цитата (символ > на початку рядка)

    Блок коду (4 пробіли на початку)
    другий рядок коду

Автоматичні посилання: https://example.com
```

### Переваги Neasden:
- Чиста типографіка (лапки, тире, пробіли)
- Підсвічування коду через highlight.js
- Таблиці, картинки з підписами
- Розширюваність через Interpreters

## 📖 Натхнення

- [x] ~~Fulltext пошук з Rose Search Engine~~
- [x] ~~Neasden Markup Parser (Markdown-like синтаксис)~~
- [x] ~~Підсвічування коду (highlight.js)~~
- [x] ~~Moment.js для красивих дат~~
- [ ] Drag & drop завантаження картинок
- [ ] RSS feed
- [ ] Фільтр по тегам
- [ ] Pagination (сторінки для стрічки)
- [ ] Форма додавання коментарів
- [ ] Простий авторизаційний механізм
- [ ] Автоматична реіндексація при створенні/редагуванні постів

## 📝 Ліцензія

Проект для навчальних цілей. Використовуйте вільно.


